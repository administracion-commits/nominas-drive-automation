name: Procesar nóminas y subir a Drive

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    # 🔴 AQUÍ SE EXPONEN LOS SECRETS AL SCRIPT
    env:
      GOOGLE_CLIENT_ID: 1066668478917-104s48sqgvbm3jfrm21g3fij4e7an6rh.apps.googleusercontent.com
      GOOGLE_CLIENT_SECRET: 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
      GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}


    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script
        run: python main.py
